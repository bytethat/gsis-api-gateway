info:
  name: Del Buyer
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8080/api/v1/gsis/39a/buyer/:vat?X-GSIS-Username={{gsis_39a_username}}&X-GSIS-Password={{gsis_39a_password}}&X-GSIS-CalledBy={{gsis_39a_calledby}}
  params:
    - name: X-GSIS-Username
      value: "{{gsis_39a_username}}"
      type: query
    - name: X-GSIS-Password
      value: "{{gsis_39a_password}}"
      type: query
    - name: X-GSIS-CalledBy
      value: "{{gsis_39a_calledby}}"
      type: query
    - name: vat
      value: "803237662"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
